# Secrets — never commit real values.
# .env.example is the committed template and is explicitly re-included.
.env
.env.*
!.env.example
*.pem

.claude/docs/commit-message.md

# Build output. scripts/package-eb.mjs writes the EB source bundle here.
dist/
