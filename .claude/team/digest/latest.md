# Digest — state of the world

Regenerated by `/digest` and by the daily scheduled run. **Every agent reads this
on spawn**, so it stays short: what changed recently, what is in flight, what is
broken. Detail lives behind the links.

Generated: **2026-08-07** · Branch `agentic-team-creation` at `00a0933`

---

## Since the last digest

- **2026-08-07** — `00a0933` committed **and pushed**. CI ran on GitHub for the
  first time and the **fast tier passed** (run `31141549342`, 1m6s). The previous
  digest's claim that CI had never run was already false when agents were reading
  it — found by `sparring` on its first invocation, which is the mechanism
  working.
- **2026-08-06** — Team completed: all 12 agents, 11 commands, the
  `AUTOMATION.md` pause switch and [`ROUTINES.md`](../ROUTINES.md). `/kickoff`
  now runs **light by default** (3 agents), `--full` for architectural work.
- **2026-08-06** — Knowledge base consolidated into
  [`.claude/docs/`](../../docs/README.md); `implementation-docs` rewritten for
  the team; ADR format added.
- **2026-08-05** — CI lint made blocking, Dependabot and CodeQL added,
  Node aligned on 24. `57fe83c`.
- **2026-08-05** — CI restructured into one orchestrated tiered pipeline.
  `88a03b1`.

## In flight

**Nine open dependency PRs, all red on `CI`, none assigned or tracked.** The
board says nothing is in flight; that is wrong, and these are the only work
actually moving.

| PR | Bump | Fails |
|---|---|---|
| #22 | eslint-config-next 15.5.3 → 16.3.0 | **fast tier** — breaking |
| #21 | eslint 9.35 → 10.8 | **fast tier** — breaking |
| #20 | lucide-react 0.544 → 1.28 | **fast tier** — breaking |
| #17 | spring-boot-starter-parent 3.5.5 → 4.1.0 | both tiers |
| #15 | maven-minor-patch group (7) | both tiers |
| #19, #18, #16, #14 | npm-minor-patch (10), actions (5), google-http-client, node 25-alpine | full tier only — likely BUG-001 |

The four full-tier-only failures are probably BUG-001 doing what it was predicted
to do. The three fast-tier failures are real breaking majors and are the ones
worth reading first.

## Broken or unverified

- **[BUG-001](../../bugs/bugs.md#bug-001)** — semantic search returns nothing for
  meaning-only matches. 1 of 40 backend tests. **Open, P0.** Not yet seen in CI
  on this branch, because the full tier has not run here.
- **The full tier has never run on `agentic-team-creation`.** Docker and the
  real-migration job were `skipped` — correct for a branch push. Opening a PR is
  what would exercise them.
- **Nothing is deployed** and no deployment target exists.
- **[BUG-003](../../bugs/bugs.md#bug-003)** — route protection not enforced. Two
  of its three stated causes are stale after the Next 16 upgrade; re-establish
  the cause before fixing.
- **[BUG-004](../../bugs/bugs.md#bug-004)** — production frontend image ships
  `NEXT_PUBLIC_*` empty. Only matters once something deploys.
- **[BUG-005](../../bugs/bugs.md#bug-005)** — public search re-embeds every query
  with no cache and no rate limit. **The repo is public and this is a working
  recipe.** Theoretical until deploy; real the moment anything ships.
- **[BUG-006](../../bugs/bugs.md#bug-006)** — no `EventService.update`, so edited
  events keep stale embeddings.

## Uncommitted work in the tree

The `/kickoff` light-mode rewrite and these state corrections are uncommitted.
Everything through `00a0933` is committed and pushed.

## Open questions heading for an ADR

- JWT transport: `localStorage` vs httpOnly cookie (BUG-003). The one item in the
  queue with a genuine design decision in it, and the intended first `--full`
  kickoff.
- Whether to adopt shadcn/ui alongside the bespoke Tailwind v4 tokens.
- Deployment target and registry.
