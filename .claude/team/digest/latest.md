# Digest — state of the world

Regenerated by `/digest` and by the daily scheduled run. **Every agent reads this
on spawn**, so it stays short: what changed recently, what is in flight, what is
broken. Detail lives behind the links.

Generated: **2026-08-06** · Branch `agentic-team-creation`

---

## Since the last digest

First digest. Recent work, newest first:

- **2026-08-06** — Knowledge base consolidated into
  [`.claude/docs/`](../../docs/README.md); `implementation-docs` skill rewritten
  for the team; ADR format added. *Staged, not committed.*
- **2026-08-06** — This team scaffold created (Phase 1).
- **2026-08-05** — CI lint made blocking, Dependabot and CodeQL added, Node
  aligned on 24. `57fe83c`.
- **2026-08-05** — CI restructured into one orchestrated tiered pipeline.
  `88a03b1`.
- **2026-08-03** — `docker compose watch` working for all three services
  ([BUG-013](../../bugs/fixed_bugs.md#bug-013)).

## In flight

Nothing assigned. See [`board/sprint.md`](../board/sprint.md).

## Broken or unverified

The things most likely to bite you if you assume otherwise:

- **[BUG-001](../../bugs/bugs.md#bug-001)** — semantic search returns nothing for
  meaning-only matches. 1 of 40 backend tests fails on this. **Open, P0.**
- **CI has never run on GitHub.** `ci/github-actions` is unpushed. When it does
  run, the full tier is *expected* to fail on BUG-001.
- **Nothing is deployed** and no deployment target exists.
- **[BUG-003](../../bugs/bugs.md#bug-003)** — route protection is half-wired;
  `proxy.tsx` is never executed by Next.js and reads a cookie while the JWT is in
  `localStorage`. Needs a decision, not a patch.
- **[BUG-004](../../bugs/bugs.md#bug-004)** — production frontend image ships
  `NEXT_PUBLIC_*` empty. Only matters once something deploys.
- **[BUG-005](../../bugs/bugs.md#bug-005)** — public search re-embeds every query
  with no cache and no rate limit.
- **[BUG-006](../../bugs/bugs.md#bug-006)** — no `EventService.update`, so edited
  events keep stale embeddings.

## Uncommitted work in the tree

The knowledge-base consolidation and this team scaffold are **staged but not
committed**. Arpan reviews and commits; agents never commit.

## Open questions heading for an ADR

- JWT transport: `localStorage` vs httpOnly cookie (BUG-003).
- Whether to adopt shadcn/ui alongside the bespoke Tailwind v4 tokens.
- Deployment target and registry.
